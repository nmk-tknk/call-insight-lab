# 論点台帳: 何が決まっていて、何が決まっていないか

- 作成日: 2026-09-18
- 位置づけ: 01〜08 と `poc/` に散在する未確定事項を 1 か所に集め、「決め手は何か・いつまでに・誰が」を付けたもの。各論点は決まり次第、該当文書に反映してここから消す。
- 読み方: §1 は再論しない決定事項。§2 が本題で、次の一手を止めている順に並べている。§3 は決定に必要な情報の一覧。

## 0. 要点

決まっていないことは 3 種類に分かれる。

1. **要件側の回答待ち(5 件)。** 環境・既存資産に関する質問で、設計者側では決められない。**実データと既知事例は現時点で使えないと回答済み(2026-09-19)。PoC は合成データで進める(§2.0)。**
2. **文書間で立場が食い違っている(3 件)。** 02 のテーマ定義エンジン、01 の PPI 補正・蒸留、02 と 07 の「通話 1 件 = 1 行」表現。放置すると後の文書がどちらを前提にしているか分からなくなる。
3. **設計者側で決められるが、実測がないと決めきれない(残り)。** 語彙進化の閾値、抽出の段数、モデル選択など。PoC の自動実行が前提。

したがって最初の一手は、合成データの設計に必要な §3 の質問への回答を得ることと、API 認証情報のある環境で合成と抽出を実行することの 2 つに絞られる。

## 1. 決定済み(再論しない)

| 決定 | 出所 | 覆す条件 |
|---|---|---|
| 作る価値はある。ただし「答えを出す装置」ではなく「定義・手順・根拠を強制する基盤」として | 02 §1, 06 §1 | 既知事例の盲検再現に 2 件以上失敗(02 §6) |
| 分析列はすべて閉じた語彙。自由記述(`summary_ja`, `other_text`)は集計に使わない | 03, 06 原則 2, 07 §1-3 | — |
| 分析単位は用件、原子単位はターン、通話は導出 | 05, 06 原則 3, 07 §1-1 | — |
| ソース層と注釈層を分ける(スタンドオフ)。注釈は `run_id` で出所を持つ | 07 §1-2 | — |
| 用件は「行為 × 対象」。`reason_l1/l2` と `touchpoint` は廃止 | 07 §1-4, 5 | 対象マスタが数千件を超え運用に耐えない場合は `object_kind` ごとに表を分ける(07 §6) |
| 根拠は位置参照+引用。逐語一致を機械検証する | 07 §1-7 | — |
| 人手ラベリングは前提にしない。品質は「証明」ではなく「担保」 | 02 §4, 06 原則 4 | — |
| 語彙は自動で育てる。人は承認者ではなく拒否権者 | 04 §4 | — |
| 感情は通話レベルのみ | 07 §1-8 | 用件レベルの感情が要件化された場合(信頼性は低い前提) |
| v0.3.1 は追加のみ。既存の表・列・ビュー・不変条件は変えない | 08 §8, `v0_3_1.sql` | — |

## 2. 未決の論点

凡例: 期限 = **今**(次の作業を止めている)/ **PoC**(自動抽出の実測後)/ **v1**(本番設計時)/ **要件時**(要件が出たら)。決定者 = 依頼者(要件・環境・データの所有者)/ 設計(本検討の担当)/ 分析者(将来の利用者)。

### 2.0 合成データで進めることの帰結(P1 の回答を受けて)

実データがないため、PoC は合成通話で行う。02 §6 の基準のうち何が測れて何が測れないかを先に固定する。

| 02 §6 の基準 | 合成データで | 備考 |
|---|---|---|
| 引用の逐語一致率 | **測れる** | 抽出器の性質なので入力が合成でも成立 |
| 合成データでの再現率・適合率(混乱、テーマ該当、境界例) | **測れる** | もともと合成前提の基準。正解ラベルは生成時に埋め込む |
| 判断列の 2 モデル一致(κ) | **測れる** | 同上 |
| 外部整合(`unresolved` → 7 日内再入電) | **測れない** | CRM 側の事実も合成すると循環する。v1 で実データが入ってから |
| 既知事例の盲検再現 | **条件付きで測れる** | 真因シナリオを埋め込んだ合成コーパスで代替。**シナリオを決める人と分析する人を分ける**(SD3)。同一人物なら「盲検」にならない |
| 分析者の根拠 30 件目視 | **測れる(弱い)** | 合成通話は生成モデルの文体に寄るため、過検出の実態は実データより楽観的に出る |
| 分析者の体感 | **測れない** | 実テーマがない |

合成データの限界も明記する。(1) 生成モデルと抽出モデルが同系なら一致率が過大評価される。生成と抽出は別モデル(または別ベンダー)にする。(2) ASR 誤りとフィラーを模擬しないと実データより易しくなる。数字・固有名詞の誤認識、言い淀み、話者の混在を意図的に注入する(SD4)。(3) 「未知の未知」は合成できない。語彙進化(04)の検証は「意図的に語彙から外したテーマを検知できるか」に限定する。

合成データの設計で決めるべきこと(新規):

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| SD1 | **模倣する業種と用件の分布。** PoC の 19 件は通信系(プラン・請求・日割り)の筋書き | 暫定: 通信系を継続。ただし将来の実データの業種が違うなら初めから合わせる | §3 Q1 | **今** / 依頼者 |
| SD2 | **規模。** 数百件(機構の確認)か 2,000〜5,000 件(02 §8 の PoC 規模、分割半分再現と語彙進化が試せる)か | 暫定: 第 1 段 300 件で抽出器を固め、第 2 段 2,000 件で急増シナリオと語彙進化を試す | §3 Q2 の予算 | **今** / 依頼者 + 設計 |
| SD3 | **真因シナリオの盲検化。** 誰が「真因」を決めて誰に伏せるか | 暫定: 依頼者(または設計以外の第三者)がシナリオ仕様を書き、設計者は生成器の骨格だけ作る。難しければ設計者がシナリオを決め、分析は別の担当が行う | §3 Q3 | **今** / 依頼者 |
| SD4 | **書き起こしの様式。** きれいな台本か、ASR 風(誤認識・フィラー・句読点なし)か。将来の実 ASR の出力形式に寄せる | 暫定: ASR 風。数字・固有名詞の誤りを一定率で注入し、ターン時刻と信頼度も模擬する | §3 Q4 | **今** / 依頼者 + 設計 |
| SD5 | **模擬する業務メタデータ。** IVR・キュー・後処理コード・CRM ケース ID のうち、将来の実データに存在するものだけ合成する | 暫定: 存在が未確認のものは合成しない(存在しない列に依存した分析を作らないため) | §3 Q5 | **今** / 依頼者 |
| SD6 | **生成モデルと抽出モデルの分離。** 同系モデルで生成と抽出を行うと一致率が過大評価される | 暫定: 生成と抽出で別モデルを使う。可能なら別ベンダー | §3 Q2 で使えるモデルの範囲 | **今** / 設計 |
| SD7 | **正解ラベルの持ち方。** 生成時のシナリオ仕様(用件・局面・混乱・真因)を v0.3.1 の表に「`annotator = 'synthetic:<gen_id>'` の run」として保存し、抽出 run と同じ SQL で比較する | 暫定: そうする。正解も注釈層の 1 run として扱えば比較クエリが増えない | — | 決定に近い / 設計 |

### 2.1 前提と要件(依頼者の回答がないと決められない)

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| P1 | ~~実データと既知事例は使えるか~~ | **回答済み(2026-09-19): 現時点では使えない。合成データで進める。** 帰結は §2.0 | — | 決定済み |
| P2 | **チャネルの範囲。** 音声のみか、チャット・メールも含むか | 暫定: 音声を主、チャットは `turns` で吸収(07 §6)。メールはターン概念が弱い | 依頼者の回答。チャットを含むなら 08 S16(添付・応答待ち)の取込設計が v0.3.1 に入る | **今** / 依頼者 |
| P3 | **ASR の出所と品質。** 既存 ASR 製品か、話者分離・タイムスタンプ・信頼度が付くか | 暫定: 話者分離とターン時刻あり、ターン別信頼度あり(`v0_3_1.sql` S6)を前提 | 依頼者の回答。信頼度が取れないなら S6 の列は空のままになり、数値誤りの検出手段を別に要する | **今** / 依頼者 |
| P4 | **個人情報と外部 API の可否。** マスキング済みか、クラウド LLM API に送れるか、国内リージョン・オンプレ制約はあるか | 暫定: マスキング済み、クラウド API 可(02 §0「LLM 利用可」)。ただし規程レベルの確認は未 | 依頼者の規程確認。オンプレ必須なら国産・オープンモデルで κ を測り直す | **今** / 依頼者 |
| P5 | **結合できる業務メタデータ。** IVR 選択、キュー、後処理コード、CRM ケース ID、CSAT の有無 | 暫定: `v0_3_1.sql` で列は用意した。取れるものだけ埋める | 依頼者の回答。後処理コードがあれば初期語彙と外部整合の両方の材料になる(01 §7-4) | **今** / 依頼者 |
| P6 | **利用者と道具。** 誰が何人で、SQL / BI / ノートブックのどれで集計するか | 暫定: SQL とノートブック、社内ツールレベル(02 §0) | 依頼者の回答。BI 前提なら `v_calls` 系のビューを増やし、ノートブック前提ならレシピを Python で書く | **v1** / 依頼者 |

### 2.2 文書間で立場が食い違っているもの

| ID | 論点 | 食い違い | 暫定の立場 | 期限 / 決定者 |
|---|---|---|---|---|
| D1 | **テーマ定義エンジンは残るか。** 02 §3-② は「分析者が自然言語の定義文を書き、`request_summary` に適用して 10 万件を再分類する」層を構成要素にしている。03 以降は「自由記述は集計に使わない」 | 定義文を要約に適用した結果は閉じた語彙ではなく、再現性の担保が `codes` の外にある | **残すが位置を変える。** 定義文は「新しいコードの候補定義」(04 §2)として語彙進化ループの入力にし、集計は採用後のコードで行う。探索用の一時分類は `annotation_runs.annotator = 'rule:theme:<id>'` の run として保存すれば出所は追える。この整理を 02 に反映するか、02 を「歴史文書」と明記するかを決める | **今** / 設計 |
| D2 | **PPI 補正と蒸留は撤回か保留か。** 01 は人手ゴールデンセット+PPI 補正と、安定列の小型モデル蒸留を推奨した。02 で人手ラベル不可となり PPI は不成立。蒸留は「当面見送り」 | 01 の推奨アーキテクチャを読んだ人が PPI・蒸留を前提にする | **PPI は撤回、蒸留は v1 のコスト論点に格下げ。** 01 §5 に注記を入れる | **今** / 設計 |
| D3 | **「通話 1 件 = 1 行」の表現。** README と 02 は「通話 1 件 = 1 行」、07 は「通話は導出」 | 入口の説明と実体が違う | README を「用件単位に構造化し、通話行は導出する」に改める | **今** / 設計 |

### 2.3 スキーマ(v0.3.1 で残った判断)

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| S1 | **対象マスタの粒度。** 「請求明細」までか、「請求明細 2026-09」までインスタンス化するか(08 C16) | 暫定: 種類まで。インスタンスは `trigger_object_code` と S11 の CRM 突合で代替 | 目的 (a) の既知事例で「どの通知か」の特定が種類だけで足りたか | **PoC** / 設計 |
| S2 | **顧客属性のスナップショット(08 S12)。** `call_customer_snapshot` を埋めるか、CRM の時点履歴に頼るか | 暫定: CRM に履歴があれば表は空のまま | 依頼者の CRM 仕様(P5 と同時に確認) | **今** / 依頼者 |
| S3 | **音声の保持(08 S13)。** `audio_uri` を埋めるか | 暫定: 埋めない。再 ASR とパラ言語の要件が出るまで | 保存コストと個人情報方針 | **要件時** / 依頼者 |
| S4 | **`agent_actions` の主キー。** (run, call, issue, action_code) は同じ行動を 2 回(2 度説明した)を 1 行に潰す | 暫定: 潰す。回数は T7 の導出で代替 | 「説明の回数」が (b) の指標として要るか | **PoC** / 設計 |
| S5 | **局面・帰属・主用件に根拠を求めるか(08 M7)。** `evidence.field` に `primary` を足したが、局面と帰属は構造検査のみ | 暫定: 局面と帰属は I1〜I3 と 2 run 一致で担保し、引用は求めない | 2 run 一致の実測で局面境界の κ が 0.7 を下回るなら、根拠を求めるより局面定義を直す | **PoC** / 設計 |
| S6 | **初期語彙の妥当性。** `v0_3_1.sql` §5 の `motive` / `agent_action` / `turn_flag` は設計者が置いた provisional | 暫定: そのまま抽出に使い、`other` 率と一致率で見直す | PoC 実測の `other` 率 | **PoC** / 設計 + 分析者 |
| S7 | **`turn_flags` の個人属性(`accessibility_need`)。** 個人属性の推定を注釈として保存してよいか | 暫定: 粗い粒度に限り、集計は件数のみ。個人単位の照会には使わない | 依頼者の個人情報方針(P4 と同時に確認) | **今** / 依頼者 |

### 2.4 処理(07 §7 で処理側へ送ったもの)

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| X1 | **局面と用件帰属の抽出段数。** 一段(1 プロンプトで局面・用件・列すべて)か、二段(局面 → 用件)か | 暫定: 未決。PoC(v0.2)は通話 1 行の一段のみで、ターン帰属は未実施 | 同一サンプルで両方式の帰属一致率とコストを比較 | **PoC** / 設計 |
| X2 | **本人確認ターンの扱い。** 抽出入力から除外するか、含めて `verification` を付けさせるか | 暫定: 含める(I3 で帰属を禁止) | 除外した方が帰属精度が上がるかの実測 | **PoC** / 設計 |
| X3 | **初期語彙の作り方。** 何件から LLM 誘導するか、既存の後処理コード(P5)から出発するか | 暫定: 後処理コードがあればそれを親にし、サンプル 300 件から子を誘導 | P5 の回答 | **今** / 設計(P5 待ち) |
| X4 | **語彙進化の閾値(04 §3)。** 規模 50 件、持続 2 期間、重複 20%、κ 0.7、凝集 80%、昇格 8 週。いずれも根拠は慣行 | 暫定: そのまま初期値にし、運用で調整 | 実データで暫定タグの採用数と拒否数を 3 か月観測 | **v1** / 設計 + 分析者 |
| X5 | **バックフィルの範囲。** 語彙変更時に `other` と近傍タグのみ再抽出(04 §5-3)で、時系列の断絶を許容できるか | 暫定: 差分のみ。`code_lineage` で親レベルの連続性を担保 | 親レベルの週次件数が語彙変更前後で変わらないことの確認 | **v1** / 設計 |
| X6 | **モデル選択とコスト。** 上位・中位・小型・国産のどれを本番に使うか。02 §5 の試算は仮定値 | 暫定: 判断列は上位、それ以外は中位、2 run 一致は判断列のみ | 同一 300 件で列ごとの κ とコストを比較 | **PoC** / 設計 |
| X7 | **鮮度。** 夜間バッチで足りるか | 暫定: 月次または日次バッチ(02 §3-①) | 依頼者の回答 | **v1** / 依頼者 |

### 2.5 品質・検証(02 §6 の基準はすべて未計測)

| ID | 論点 | 現状 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| Q1 | **手動抽出との一致(κ)。** `poc/samples/extracted.jsonl` はモデルの手動生成で、`extract.py` は未実行 | 未計測。PoC の数値はすべて「機構が回る」ことの確認に留まる | API 認証情報のある環境で `extract.py` を実行(合成データの生成にも同じ認証情報が要る) | **今** / 依頼者(環境)+ 設計 |
| Q2 | **indirect 混乱の過検出。** LLM を使う意味の中核であり、最も過検出しやすい(02 §2.2) | 未計測 | 根拠 30 件目視(02 §4)を実テーマ 2 本で実施。合格 0.8 | **PoC** / 分析者 |
| Q3 | **系統バイアス。** 日本語の丁寧な確認表現を一律「混乱」と判定する等。2 run 一致では検出できない | 対策は合成データの境界例と目視のみ | 境界例を含む合成データ数百件の作成 | **PoC** / 設計 |
| Q4 | **外部整合の分母。** `unresolved` の 7 日内再入電率が「全体の 1.5 倍以上」(02 §6)。PoC では件数が少なすぎて判定不能 | 未計測。**合成データでは測れない(§2.0)** | 実データと P5 の CRM ケース ID(S11) | **v1** / 設計 |
| Q5 | **合格ラインの根拠。** 逐語一致 98%、κ 0.7、再現率 0.9 は慣行値 | 根拠なし | 実測の分布を見て、意思決定に足る幅かを依頼者と決める | **PoC** / 依頼者 + 設計 |

### 2.6 分析レシピ(未実装)

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| R1 | **期間の決め方。** `v_issues_with_period`(07 §5)は未定義。急増期間と平常期間を人が指定するか、変化点検知で自動提案するか | 暫定: 人が指定し、変化点検知は提案のみ | 既知事例で自動提案が真の変化点を当てるか | **PoC** / 設計 |
| R2 | **分母。** 用件数か通話数か。テーマ該当の割合は分母で変わる | 暫定: 両方出す(07 §5) | — | 決定済みに近い。レシピ実装時に固定 |
| R3 | **効果量と信頼区間。** 期間比較の差分に何を使うか(比率差、オッズ比、Wilson 区間) | 暫定: 比率差と Wilson 区間。LLM 誤差は別枠(02 §4) | 分析者の読みやすさ | **v1** / 設計 + 分析者 |
| R4 | **未説明残余の表し方。** 仮説で説明できない急増分をどう数値化して見せるか | 暫定: 仮説該当を除いた残りの時系列を並べる | 既知事例での試作 | **PoC** / 設計 |
| R5 | **どこまでを「レシピ」が強制するか。** 分母・比較期間・両側引用・残余を省略不可にする(02 §3-③)と、探索の自由度が落ちる | 暫定: 共有・意思決定資料に使う出力にのみ強制。探索は自由だが「未確認」ラベル | 分析者の受容 | **v1** / 分析者 |

### 2.7 運用・ガバナンス

| ID | 論点 | 選択肢と暫定の立場 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| G1 | **拒否権を持つ人。** 週次ダイジェストを誰が見て、拒否・改名・統合を誰が指示するか(04 §4) | 暫定: 分析者 1〜2 名。未指名 | 依頼者の体制 | **v1** / 依頼者 |
| G2 | **根拠引用と要約の個人情報。** `evidence.quote` と `summary_ja` は原文の一部を含む。マスキング済み前提だが、要約・クラスタ名からの復元リスク(01 §3.6 Cliopatra)は残る | 暫定: マスキングをソース層で保証し、注釈層は追加対策なし | P4 の規程確認 | **今** / 依頼者 |
| G3 | **語彙の年次棚卸し。** 近縁タグの増殖(04 §7)を誰がいつ見直すか | 暫定: 年 1 回、分析者 | — | **v1** / 分析者 |
| G4 | **リポジトリの位置づけ。** 検討メモと PoC の置き場か、本番コードベースの起点か(01 §7-12) | 暫定: 検討メモと PoC。本番は別途 | 依頼者の回答 | **v1** / 依頼者 |

## 3. 依頼者への質問(2026-09-19 改訂。実データ不可を受けて書き直し)

回答済み: 実データと既知事例は現時点では使えない。合成データで進める。

以下は、合成データの設計と実行に**今**必要なもの(Q1〜Q5)と、v1 の設計まで待てるもの(Q6〜Q7)に分けた。各問いは選択肢または具体値で答えられる形にしてある。「不明」も回答として有効で、その場合の暫定値を併記する。

### 今必要(合成データの設計に直結)

**Q1. 模倣する業種と問い合わせの種類**(SD1)
- Q1-a. 将来の実データの業種はどれか: 通信 / 金融・保険 / 電力・ガス / EC・小売 / 公共 / その他(具体名)。不明なら PoC の通信系を継続する。
- Q1-b. 主要な問い合わせの種類を、多い順に 5〜10 個挙げられるか(例: 請求金額、解約、住所変更、ログイン不可)。挙げられなければ、通信系の一般的な分布を設計側で置く。
- Q1-c. 目的 (b)「わかりづらい」の対象として実際に問題になっているもの(書面名、画面名、用語)を 3 つ以上挙げられるか。なければ PoC の「日割り」「改定通知」を継続する。

**Q2. LLM API の利用条件**(P4, X6, SD2, SD6)
- Q2-a. この PoC 用に API 認証情報を用意できるか: はい(このリポジトリの実行環境に設定できる)/ はい(依頼者の手元でのみ実行)/ いいえ。「いいえ」なら合成データも抽出も手動生成になり、κ は測れない。
- Q2-b. 使えるモデル・ベンダーの範囲: 単一ベンダー / 複数ベンダー可。生成と抽出を別モデルにしたいので(SD6)、複数可かどうかが効く。
- Q2-c. PoC の費用上限: 概算でよいので月額の上限(円またはトークン数)。この値で SD2 の規模(300 件 / 2,000 件)を決める。費用の見積りは規模が決まってから設計側で出す。
- Q2-d. 実行場所の制約: クラウド API 可 / 国内リージョン限定 / オンプレのみ。合成データには個人情報がないので PoC 段階では制約が緩いはずだが、v1 と同じ条件で測るべきなので確認する。

**Q3. 盲検テストの体制**(SD3)
- Q3-a. 合成コーパスに埋め込む「真因シナリオ」の仕様を、設計者以外の人(依頼者本人または第三者)が書けるか: 書ける / 書けない。
- Q3-b. 書けない場合、設計者がシナリオを決め、分析は別の担当が行う体制を取れるか: 取れる / 取れない(同一人物で行い「盲検ではない」と明記する)。

**Q4. 将来の実データの書き起こし形式**(P2, P3, SD4)
- Q4-a. チャネル: 音声のみ / 音声+チャット / 音声+チャット+メール。合成するチャネルをこれに合わせる。
- Q4-b. ASR 製品名(分かれば)と、出力に含まれるもの: 話者分離(あり / なし / 不明)、ターン時刻(あり / なし / 不明)、信頼度(通話単位 / 単語単位 / なし / 不明)。不明なら「話者分離あり、ターン時刻あり、信頼度なし」を暫定にする。
- Q4-c. 書き起こしの状態: 句読点あり / なし、フィラー(「えー」「あの」)が残る / 除去済み、数字の表記(漢数字 / 算用数字 / 混在)。不明なら「句読点なし、フィラー残存、混在」で生成する。

**Q5. 将来の実データに存在する業務メタデータ**(P5, S2, SD5)
それぞれ「ある / ない / 不明」で答える。「ある」ものだけ合成し、v0.3.1 の該当列を使う。
- Q5-a. IVR の選択履歴
- Q5-b. 着信キュー名と待ち時間
- Q5-c. 通話の終了理由(顧客切断 / オペレータ終了 / 途中切断)
- Q5-d. オペレータが入力する後処理コード(ディスポジション)
- Q5-e. CRM のケース ID(同一顧客の再入電を突合できるもの)
- Q5-f. 契約属性(プラン、契約年数)の**時点履歴**(過去のある日の値を引けるか)
- Q5-g. CSAT などのアンケート

### v1 まで待てる(合成 PoC には不要)

**Q6. 個人情報の扱い**(P4, S7, G2)
- Q6-a. マスキングを行う主体と段階: ASR ベンダー側 / 自社の取込時 / 未定。
- Q6-b. マスキング済みの書き起こしを外部 LLM API に送ってよいか: 可 / 不可 / 規程の確認が必要。
- Q6-c. 「利用上の制約」(視力、言語、年齢など)を注釈として保存してよいか: 可(件数集計のみ)/ 不可。

**Q7. 利用者と運用**(P6, G1, X7)
- Q7-a. 集計する人の人数と道具: 人数、SQL / BI(製品名)/ ノートブック / Excel のいずれか。
- Q7-b. 語彙の週次ダイジェストを見て拒否権を行使する担当を指名できるか: できる(役割名)/ 未定。
- Q7-c. 鮮度: 月次 / 日次 / 当日中。

## 4. 設計側が今すぐ決めるもの(依頼者の回答を待たない)

- D1: テーマ定義エンジンを語彙進化ループの入力に位置づけ直し、02 に注記する
- D2: 01 §5 に PPI 撤回・蒸留格下げの注記を入れる
- D3: README の「通話 1 件 = 1 行」を改める
- X1, X2: `extract.py` を v0.3.1 対応(ターン帰属・局面・新列)に書き換え、一段・二段と本人確認ターンの有無を比較できる形にする。実行は Q2 待ち
