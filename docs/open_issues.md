# 未決の論点と、依頼者への確認事項

- 出所: 旧 docs/study/09(論点台帳)§2〜4 から、決定済みの行を除いて移した(2026-09-22)。決定は各文書(01〜06)の決定の書式で、経緯は [history.md](history.md)。
- 凡例: 期限 = **今**(次の作業を止めている)/ **PoC**(第 1 段の実測後)/ **v0.4.1**(次のスキーマ版)/ **v1**(本番設計時)。決定者 = 依頼者 / 設計 / 分析者(将来の利用者)。

## 1. 依頼者への確認事項

未回答なら括弧内の既定値で進める。

| # | 確認事項 | 既定値 | 関連 |
|---|---|---|---|
| A1 | 社内で「特定のテーマ」と呼ばれるもののうち、[04_analysis_needs.md](04_analysis_needs.md) §2.1 の 7 族で書けない例があるか。値の粒度が足りない族はどれか。**保留**(2026-09-22): 依頼者が 14 例を提示。Claude の判定(13 例は書ける、代理連絡は置き場がない、6 例は値・対象が足りない)は S17 の結論を待って再検討 | — | 目録、S17 |
| A2 | 書き起こしファイルにターン時刻(または通話長)、オペレータ ID、顧客 ID が含まれるか | いずれも含まれない | 04 §3 D1〜D3、02 §7 |
| A3 | 費用の桁: PoC 一度きり 300 USD 未満、本番 月 300〜1,400 USD で問題ないか | 問題なし | [06_cost.md](06_cost.md) |
| A4 | 複数ベンダーの API を使えるか(生成と抽出の分離、2 run の別ベンダー化) | 使える | SD6 |
| A5 | 合成コーパスの用件比率と混乱率([05_poc_plan.md](05_poc_plan.md) §2.2〜2.3)に、業務感覚で直したい箇所があるか | そのまま | SD10 |
| A6 | 既知の誤解一覧 8 件([02_schema.md](02_schema.md) §5)の正誤と文言、保守担当 | 分析者が語彙と同じ手順で保守 | S12 |
| A7 | 「発話行為は問いの有無によらず全種類を抽出する」([01_concept.md](01_concept.md) M10 からの帰結として提示中)の承認 | 承認として進める | 02 §4.1 |
| A8 | 問い 9(顧客が述べた金額の分布)の集計を作るか(抽出は常に行う) | 作らない | 目録 |

## 2. 未決の論点

### 2.1 スキーマ(v0.4.1 で追加するもの)

| ID | 論点 | 暫定 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| S17 | **語彙の不足をどう扱うか。** 2026-09-22 に依頼者が「語彙に該当がない」を常態とする前提に疑義([01_concept.md](01_concept.md) M11)。論点: (1) 対象・値を抽出前に業務側の目録から完成させるか、通話から育てるか。(2) 主張コード(業種固有の誤解一覧)を注釈に持ち込む方式を続けるか。(3) 値を足すときの費用(全件再抽出か、該当行の引用の再分類か)。(4) A1 の 14 例のうち値・対象が足りなかった 6 例の扱い | 議論は地盤固めの後。それまで 02 §4.1・§5、03 §2 は案のまま | 依頼者との議論 | **今** / 依頼者 + 設計 |
| S18 | **相槌ターンの扱い。** 実データでは相槌が途中で頻繁に挟まる([01_concept.md](01_concept.md) R4)。相槌だけのターンを用件に帰属させるか(直前の用件を引き継ぐ / 帰属なし)、局面をどう付けるか、発話行為の抽出入力から除くか、1 発話が相槌で分断されたときの引用の扱い | 直前の用件を引き継ぎ、抽出入力からは除く(案) | 合成データに相槌を頻繁に挿入し、帰属の 2 run 一致と引用の逐語一致で確認 | **PoC** / 設計 |
| S14 | **業務側の変更の登録先**(目録 #13)。郵送物の改版、サービス・手続きの変更、料率改定、キャンペーンの「いつ・何が」を業務知識表 `business_changes` に登録し、前後比較のレシピが参照する | 追加する。既知の誤解一覧と同じく業務側が保守 | — | **v0.4.1** / 設計 |
| S15 | **経路**(目録 #17)。「経緯を述べる」を 1 用件に複数持ち、順序で並べる導出ビュー。きっかけの語彙に「Web・マイページで試した」を追加 | 追加する | — | **v0.4.1** / 設計 |
| S16 | **未解決の理由**(目録 #45、依頼者追加)。オペレータの「断った」「案内した」に理由・案内先の値を持たせる(代理店専用手続き / 本人以外 / 書類不足 / 権限外、案内先 = 代理店 / Web / 郵送 / 他部署)。導出に未解決の理由を足す | 追加する | — | **v0.4.1** / 設計 |
| S1 | **対象マスタの粒度。** 「更新案内」までか、「更新案内 2026 年 9 月様式」のように版でインスタンス化するか | 種類まで。業務側の変更は S14 で持つ | 封印シナリオで「どの通知か」の特定が種類だけで足りたか | **PoC** / 設計 |
| S3 | **音声の保持。** `audio_uri` を埋めるか | 埋めない。再 ASR とパラ言語の要件が出るまで | 保存コストと個人情報方針 | 要件時 / 依頼者 |
| S5 | **局面・帰属に根拠を求めるか。** 構造は引用を持たず、参照整合と 2 run 一致で担保している | 求めない | 局面境界の κ が 0.7 を下回るなら、根拠を求めるより局面定義を直す | **PoC** / 設計 |
| S6 | **初期語彙の妥当性。** 発話行為の値(動機、主張、側面など)は設計者が置いた provisional | そのまま抽出に使い、`other` 率と一致率で見直す | 第 1 段の `other` 率 | **PoC** / 設計 + 分析者 |
| S13 | **v0.4 で失う量の許容水準。** 動機・受容・結果の「不明」率がどこまでなら実運用で許容できるか | 第 1 段で実測してから決める | [05_poc_plan.md](05_poc_plan.md) §5 の実測 | **PoC** / 依頼者 + 設計 |

### 2.2 合成データ・処理

| ID | 論点 | 暫定 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| SD2 | **規模。** 第 1 段 300 件、第 2 段 2,000 件 | そのまま | A3 の回答 | **今** / 依頼者 + 設計 |
| SD6 | **生成モデルと抽出モデルの分離。** 同系だと一致率が過大評価される | 別モデル。可能なら別ベンダー | A4 の回答 | **今** / 設計 |
| SD8 | **生成用モデル。** 外部 API の中位〜上位(Sonnet 5 で約 76 USD、Opus 5 で約 190 USD)。抽出に Claude を使うなら生成は他社 | 外部 API | A3 の回答 | **今** / 依頼者 |
| SD9 | **抽出用モデルの候補。** [06_cost.md](06_cost.md) §A5 の案 1(中位単一)で開始し、第 1 段の κ で発話行為を小型に落とす | 案 1 | 第 1 段 300 件 × 5 モデルの κ | **PoC** / 設計 |
| SD10 | **用件比率と混乱率の妥当性。** 設計者の推定 | そのまま生成し、依頼者の業務感覚で補正 | A5 の回答 | **今** / 依頼者 |
| MK1 | **マスキングの方式と前データの保持。** 取込時にローカルで規則 + NER。マスキング前は保持しない | そのまま | 第 1 段のマスキング再現率 | **PoC** / 設計 |
| X1 | **抽出の段数。** 一段(1 プロンプトで構造と発話行為)か、二段(構造 → 発話行為)か。v0.4 では二段が自然(構造は中位、発話行為は小型) | 二段を第一候補 | 同一サンプルで一致率と費用を比較 | **PoC** / 設計 |
| X2 | **本人確認ターンの扱い。** 抽出入力から除外するか、含めて `verification` を付けさせるか | 含める(I3 で帰属を禁止) | 除外した方が帰属精度が上がるかの実測 | **PoC** / 設計 |
| X3 | **初期語彙の作り方。** 合成 PoC では対象マスタ草案を使う。v1 で実データが入ったら 300 件から LLM 誘導し、草案との差を見る | 草案 | v1 の実データ | **v1** / 設計 |
| X4 | **語彙進化の閾値。** 規模 50 件、持続 2 期間、重複 20%、κ 0.7、凝集 80%、昇格 8 週。いずれも慣行値 | 初期値のまま運用で調整 | 実データで暫定値の採用数と拒否数を 3 か月観測 | **v1** / 設計 + 分析者 |
| X5 | **バックフィルの範囲。** 語彙変更時に `other` と近傍の値のみ埋め直すことで、時系列の断絶を許容できるか | 差分のみ。親レベルの連続性は写像表で担保 | 親レベルの週次件数が変更前後で変わらないことの確認 | **v1** / 設計 |
| X7 | **鮮度。** 夜間バッチで足りるか | 月次または日次バッチ | 依頼者の回答 | **v1** / 依頼者 |

### 2.3 品質・検証(すべて未計測)

| ID | 論点 | 現状 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| Q1 | **抽出の実測。** 旧 PoC(v0.2)の抽出結果はモデルの手動生成で、自動抽出は未実行 | 未計測 | API 認証情報のある環境で第 1 段を実行 | **今** / 依頼者(環境)+ 設計 |
| Q2 | **間接的な混乱の過検出。** v0.4 では「主張 + 訂正」の規則に置き換わったが、主張の抽出自体の過検出は残る | 未計測 | 根拠 30 件目視を実テーマ 2 本で実施。合格 0.8 | **PoC** / 分析者 |
| Q3 | **系統バイアス。** 日本語の丁寧な確認表現を一律「理解困難の表明」と取る等。2 run 一致では検出できない | 対策は合成データの境界例と目視のみ | 境界例を含む合成データ数百件 | **PoC** / 設計 |
| Q4 | **外部整合。** 未解決の 7 日内再入電率。合成データでは測れない | 未計測 | 実データと顧客 ID | **v1** / 設計 |
| Q5 | **合格ラインの根拠。** 逐語一致 98%、κ 0.7、再現率 0.9 は慣行値 | 根拠なし | 実測の分布を見て、意思決定に足る幅かを依頼者と決める | **PoC** / 依頼者 + 設計 |

### 2.4 分析レシピ(未実装)

| ID | 論点 | 暫定 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| R1 | **期間の決め方。** 急増期間と平常期間を人が指定するか、変化点検知で自動提案するか | 人が指定し、変化点検知は提案のみ | 封印シナリオで自動提案が真の変化点を当てるか | **PoC** / 設計 |
| R2 | **分母。** 用件数か通話数か | 両方出す | — | レシピ実装時に固定 |
| R3 | **効果量と信頼区間。** 比率差、オッズ比、Wilson 区間 | 比率差と Wilson 区間。LLM 誤差は別枠 | 分析者の読みやすさ | **v1** / 設計 + 分析者 |
| R4 | **未説明残余の表し方。** 仮説で説明できない急増分をどう数値化して見せるか | 仮説該当を除いた残りの時系列を並べる | 封印シナリオでの試作 | **PoC** / 設計 |
| R5 | **どこまでを「レシピ」が強制するか。** 分母・比較期間・両側引用・残余を省略不可にすると探索の自由度が落ちる | 共有・意思決定資料に使う出力にのみ強制。探索は自由だが「未確認」ラベル | 分析者の受容 | **v1** / 分析者 |

### 2.5 運用・ガバナンス

| ID | 論点 | 暫定 | 決め手 | 期限 / 決定者 |
|---|---|---|---|---|
| P6 | **利用者と道具。** 誰が何人で、SQL / BI / ノートブックのどれで集計するか | SQL とノートブック、社内ツールレベル | 依頼者の回答(今は不要) | **v1** / 依頼者 |
| G1 | **拒否権を持つ人。** 週次ダイジェストを誰が見て、拒否・改名・統合を誰が指示するか | 分析者 1〜2 名。未指名 | 依頼者の体制 | **v1** / 依頼者 |
| G2 | **根拠引用と要約の個人情報。** マスキングをソース層で保証し、注釈層はマスキング後の本文しか見ない | マスキング前は保持しない。要約からの復元リスクは残る | MK1 の再現率 | **PoC** / 設計 |
| G3 | **語彙の年次棚卸し。** 近縁の値の増殖を誰がいつ見直すか | 年 1 回、分析者 | — | **v1** / 分析者 |
| G4 | **リポジトリの位置づけ。** 検討メモと PoC の置き場か、本番コードベースの起点か | 検討メモと PoC。本番は別途 | 依頼者の回答 | **v1** / 依頼者 |
| H1 | **編集ガードの誤検知。** `2>&1` やコミットメッセージ内の `<...>` などの `>` を書き込みと見ること | ガードは緩めず、コマンドの書き方で回避(コミットメッセージはファイルから読む) | 依頼者の承認を得て修正 | **今** / 設計 |

## 3. 次の作業

[05_poc_plan.md](05_poc_plan.md) §6 を参照。生成器の実装、抽出スクリプトの v0.4 対応、第 1 段 300 件の実測、v0.4.1(S14〜S16)。
