# call-insight-lab — 作業規則(ハーネス)

このファイルは Claude Code が本リポジトリで従う規則である。根拠は docs/decisions/0010-harness.md。

## 1. 発言の種別を必ず付ける

設計・方針に関する発言には、先頭に次のいずれかを付ける。

- **決定**: 依頼者が承認した内容。決定記録(docs/decisions/)に存在するものだけ。
- **提案**: 承認を求める案。決定記録の草案として提示する。
- **仮説**: 実測や確認で確かめる前の見立て。
- **訂正**: 以前の発言を覆すもの。何を、どの発言から覆すかを必ず書く。

文書(docs 配下)に書けるのは決定だけ。提案・仮説を文書に書かない。

## 2. 議論ターンと適用ターンを分ける

- 依頼者の発言に「適用」「承認」「反映して」の明示がない限り、docs 配下の文書を編集しない。議論の結果は決定記録の草案(状態: 草案)として提示するか、会話で述べるに留める。
- 承認されたら、次の手順で一括して適用する。(1) 決定記録の状態を承認済みにする。(2) `docs/decisions/APPLY` に `adr: NNNN` と `date: YYYY-MM-DD`(当日)を書く。(3) 記録の「影響を受ける文書」だけを編集し、文書間の整合を取る。(4) APPLY を削除する。(5) コミットする。
- 1 コメントごとに部分修正しない。複数の指摘は 1 つの決定記録にまとめ、1 回で適用する。
- hook(scripts/harness/guard_docs.py)が上記を機械的に検査する。拒否されたら回避せず、依頼者に承認を求める。

## 3. 決定記録

- **有効な決定の結論は docs/decisions/DECISIONS.md の 1 ファイルだけ。** 設計・方針を判断するときは、まずこの文書を読み、docs/01〜06 の記述はその説明として読む。両者が食い違えば DECISIONS.md が正。
- 番号付きの記録(docs/decisions/NNNN-*.md)は提案と承認の履歴。docs/decisions/README.md の規則に従う。承認済みの記録は書き換えない。覆すときは新しい草案を出し、承認後に DECISIONS.md を書き換える。
- DECISIONS.md に書けるのは、依頼者の発言で裏付けられることだけ。承認欄と根拠発言欄には依頼者の言葉を引用で書く(要旨に言い換えない)。設計者の付加は DECISIONS.md §5「決定ではないもの」に分けて書く。
- docs/open_issues.md は未決の論点と依頼者への確認事項の一覧。

## 4. 文書の整合

- 原則は docs/01_concept.md にだけ書き、他の文書はそれを参照する。同じ原則を複数の文書に書き写さない。
- 文書を編集したら、同じ適用ターンで関連文書(README の説明文、open_issues、decisions/README の索引)も整合させる。新旧の判定や用語が 1 文書に混在する状態でターンを終えない。
- 取り消し線には日付を付ける。件数を書く集計表は本文の行数と一致させる。

## 5. コミット

- 適用ターンの終わりに 1 コミット。メッセージに決定記録の番号を書く。
- 議論ターンではコミットしない(決定記録の草案の追加は例外)。
