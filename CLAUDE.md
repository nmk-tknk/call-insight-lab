# call-insight-lab — 作業規則(ハーネス)

このファイルは Claude Code が本リポジトリで従う規則である。経緯は docs/history.md。

## 1. 発言の種別を必ず付ける

設計・方針に関する発言には、先頭に次のいずれかを付ける。

- **決定**: 依頼者が承認した内容。docs の決定の書式(§3)で書かれているものだけ。
- **提案**: 承認を求める案。チャットで述べる。
- **仮説**: 実測や確認で確かめる前の見立て。
- **訂正**: 以前の発言を覆すもの。何を、どの発言から覆すかを必ず書く。

依頼者の質問は意向ではない。質問には利害得失を答えて推奨を保ち、推奨を変えるのは新しい事実が出たときだけにする。

## 2. チャットで承認、ファイルに反映

- 議論も提案もチャットで行う。提案のためのファイルは作らない。
- 依頼者がチャットで承認(「承認」「適用」「反映して」、または同じ意味の指示)を述べるまで、docs 配下の文書を編集しない。
- 承認を受けたら、同じターンで次を行う。(1) `docs/APPLY` に `date: YYYY-MM-DD`(当日)と `承認: 「依頼者の発言の引用」` を書く。(2) 文書を編集し、文書間の整合を取る。(3) `docs/history.md` に承認の行を追記する。(4) APPLY を削除する。(5) 1 コミット。メッセージに承認の発言を引用する。
- 却下・撤回・保留になった提案は、承認がなくても `docs/history.md` に追記する。未決になった論点は `docs/open_issues.md` に追記する。この 2 ファイルだけは APPLY なしで書ける。
- 1 コメントごとに部分修正しない。複数の指摘は 1 回の承認で 1 回に反映する。
- hook(scripts/harness/guard_docs.py)が上記を機械的に検査する。拒否されたら回避せず、依頼者に承認を求める。

## 3. 決定の置き場と書式

- 決定は主題の文書に置く。概念と原則は docs/01_concept.md、スキーマは 02、語彙は 03、目録は 04、PoC は 05、費用は 06。決定を別のファイルに集めない。
- 決定の書式は固定する。表なら「決定 / 根拠発言 / 日付」の列、本文なら段落末に「(根拠: 「依頼者の発言の引用」、日付)」。根拠発言は依頼者の言葉の引用であり、要旨に言い換えない。
- 根拠発言で裏付けられないことは決定の書式で書かない。設計者(Claude)の案は「案」と明示し、決定と混ぜない。依頼者が疑義を示した案には、その旨と history.md の日付を添える。
- 設計・方針を判断するときは、まず該当文書の決定を読み、案はその説明として読む。
- docs/history.md は時系列の履歴(承認・却下・撤回・保留)、追記のみ。docs/open_issues.md は現在の未決の一覧。

## 4. 文書の整合

- 原則は docs/01_concept.md にだけ書き、他の文書はそれを参照する。同じ原則を複数の文書に書き写さない。
- 文書を編集したら、同じ適用ターンで関連文書(README の説明文、open_issues、history)も整合させる。新旧の判定や用語が 1 文書に混在する状態でターンを終えない。
- 取り消し線には日付を付ける。件数を書く集計表は本文の行数と一致させる。

## 5. コミット

- 適用ターンの終わりに 1 コミット。メッセージに承認の発言を引用する。
- 議論ターンではコミットしない(history.md と open_issues.md への追記は例外)。
